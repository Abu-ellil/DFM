# دليل استيراد البيانات
## DFM - Date Factory Management

---

## 📅 نظام السيزون - مهم!

**قبل الاستيراد:** تأكد من تفعيل السيزون المطلوب

1. افتح التطبيق وانتقل إلى صفحة **المواسم**
2. أنشئ سيزون جديداً (مثلاً: "موسم 2025")
3. فعل السيزون قبل الاستيراد

💾 **جميع البيانات المستوردة ستُربط بالسيزون النشط!**

لمزيد من المعلومات، راجع `SEASON_GUIDE.md`

---

## 📋 السكريبتات المتاحة

### 1. المراجعة الأولية `pre_import_review.js`
**الغرض:** فحص البيانات والكشف عن المشاكل قبل الاستيراد

```bash
node pre_import_review.js
```

**يقوم بـ:**
- فحص التواريخ غير الطبيعية
- فحص المراجع المعطلة (عملاء غير موجودين)
- فحص صحة المبالغ الحسابية
- فحص الصناديق
- فحص التكرار في الأسماء

---

### 2. الاستيراد الفعلي `import_data.js`
**الغرض:** استيراد البيانات من قاعدة البيانات القديمة إلى الجديدة

```bash
node import_data.js
```

**يقوم بـ:**
- استيراد جميع الجداول
- استبعاد السجلات المشكوك فيها تلقائياً
- التحقق من المراجع
- إنشاء نسخة احتياطية
- اختيار عملاء عشوائيين لمراجعة حساباتهم

---

## 🚀 خطوات الاستيراد الموصى بها

### الخطوة 1: المراجعة الأولية
```bash
cd D:\DEV\DateS\LATEST\DFM
node pre_import_review.js
```

راجع التقرير وحل أي مشاكل حرجة.

### الخطوة 2: الاستيراد
```bash
node import_data.js
```

سيقوم السكريبت بـ:
1. إنشاء نسخة احتياطية من قاعدة البيانات الحالية
2. استيراد البيانات مع معالجة المشاكل
3. عرض تقرير مفصل
4. مراجعة حسابات 5 عملاء عشوائيين

### الخطوة 3: التحقق
افتح التطبيق وتأكد من:
- ظهور العملاء بشكل صحيح
- سجلات الوزن سليمة
- الأرصدة المالية صحيحة

---

## ⚙️ خيارات الاستيراد

يمكن تعديل الخيارات في `import_data.js`:

```javascript
const IMPORT_OPTIONS = {
  // استبعاد السجلات قبل هذا التاريخ
  SKIP_OLD_DATES: true,
  MIN_VALID_DATE: '2020-01-01',

  // التحقق من المراجع
  VALIDATE_REFERENCES: true,

  // الاستمرار رغم الأخطاء
  CONTINUE_ON_ERROR: true
};
```

---

## 📁 الملفات

| الملف | الوصف |
|-------|-------|
| `date_factory_v2_2026-02-25_22-20-30.db.sqlite` | قاعدة البيانات القديمة (المصدر) |
| `date_factory_v2.db` | قاعدة البيانات الحالية (الهدف) |
| `date_factory_v2_backup_[timestamp].db` | النسخة الاحتياطية |
| `pre_import_review.js` | سكريبت المراجعة |
| `import_data.js` | سكريبت الاستيراد |

---

## 🛡️ النسخ الاحتياطي

يتم إنشاء نسخة احتياطية تلقائياً قبل الاستيراد باسم:
```
date_factory_v2_backup_[timestamp].db
```

للاستعادة:
```bash
# احذف قاعدة البيانات الحالية
del date_factory_v2.db

# أعد تسمية النسخة الاحتياطية
ren date_factory_v2_backup_[timestamp].db date_factory_v2.db
```

---

## ⚠️ المشاكل المعروفة وحلولها

### 1. السجل بتاريخ 2015-11-04
**المشكلة:** سجل واحد بتاريخ قديم جداً

**الحل:** السكريبت يستبعده تلقائياً

### 2. الصناديق المسترجعة = 0
**المشكلة:** جميع الصناديق المسترجعة مسجلة كصفر

**الحل:** تحقق من صحة البيانات الأصلية

### 3. مراجع معطلة
**المشكلة:** سجلات تشير لعملاء غير موجودين

**الحل:** السكريبت يستبعدها تلقائياً

---

## 📊 بعد الاستيراد

1. **شغل التطبيق**
   ```bash
   npm run dev
   ```

2. **افتح صفحة العملاء**
   - تأكد من ظهور جميع العملاء
   - تحقق من الأنواع (تاجر / عميل عادي)

3. **افتح صفحة الوزن**
   - تأكد من ظهور السجلات
   - تحقق من المبالغ

4. **افتح صفحة المالية**
   - تأكد من سجلات المدفوعات

5. **مراجعة الأرصدة**
   - افتح تفاصيل أي عميل
   - تحقق من رصيده

---

## 🔧 استكشاف الأخطاء

### خطأ: "Database not initialized"
**الحل:** تأكد من أن ملف `date_factory_v2.db` موجود

### خطأ: "Cannot find module 'sql.js'"
**الحل:** تأكد من تثبيت المكتبات
```bash
npm install
```

### السجلات لا تظهر
**الحل:** تحقق من التطبيق وجرب إعادة التشغيل

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع تقرير `pre_import_review.js`
2. تحقق من سجل الأخطاء في `import_data.js`
3. راجع النسخة الاحتياطية

---

*تم إنشاء هذا الدليل لمساعدة في استيراد البيانات بشكل آمن*
